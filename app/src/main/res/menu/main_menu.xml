<?xml version="1.0" encoding="utf-8"?>
<menu
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/MainMenu.history"
        android:title="@string/MainMenu.history_text"
        app:showAsAction="never" />

    <item
        android:id="@+id/MainMenu.settings"
        android:title="@string/MainMenu.settings_text"
        app:showAsAction="never" />

    <item
        android:id="@+id/MainMenu.about"
        android:title="@string/MainMenu.about_text"
        app:showAsAction="never" />

    <item
        android:id="@+id/MainMenu.refresh"
        android:title="@string/MainMenu.refresh_text"
        android:icon="@drawable/ic_cached_white"
        app:showAsAction="ifRoom"/>

</menu>
